{
	"name": "build-cleaner",
	"version": "0.1.0",
	"description": "Node.js API for build-cleaner",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"build-cleaner",
		"clean",
		"cleanup"
	],
	"author": "xjiaxiang",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.0.0"
	},
	"dependencies": {}
}
