{
	"name": "@build-cleaner/node",
	"version": "0.1.0",
	"description": "Node.js API for build-cleaner - A fast tool to clean temporary files and directories in various project types",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"bin": {
		"build-cleaner-node": "./dist/cli.js"
	},
	"files": [
		"dist",
		"README.md"
	],
	"scripts": {
		"build": "tsc && chmod +x dist/cli.js",
		"prepublishOnly": "pnpm build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"build-cleaner",
		"clean",
		"cleanup",
		"node_modules",
		"build",
		"dist",
		"temporary",
		"files"
	],
	"author": "xjiaxiang",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/xjiaxiang/build-cleaner.git",
		"directory": "npm"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.0.0"
	},
	"dependencies": {},
	"engines": {
		"node": ">=22.0.0"
	}
}
